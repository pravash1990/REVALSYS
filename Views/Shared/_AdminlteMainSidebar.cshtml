@model Models.ApplicationUser
@inject Data.ApplicationDbContext Context

@{
    UserProfile profile = Context.UserProfile.SingleOrDefault(x => x.ApplicationUserId.Equals(Model.Id));
}
<aside class="main-sidebar">
    <!-- sidebar: style can be found in sidebar.less -->
    <section class="sidebar">
        <!-- Sidebar user panel -->
        <div class="user-panel">
            <div class="pull-left image" >
                @* class="img-circle"  *@
                <img src="@profile.ProfilePicture" alt="User Image"style="height:45px; border-radius:50%">
            </div>
            <div class="pull-left info">
                <p>@profile.FirstName @profile.LastName</p>
                <a href="#"><i class="fa fa-circle text-success"></i> Online</a>
            </div>
        </div>

        <!-- sidebar menu: : style can be found in sidebar.less -->
        <ul class="sidebar-menu" data-widget="tree">
            <li class="header">DASHBOARD</li>

            <li class="@Html.IsSelected(controller: MainMenu.Dashboard.ControllerName)">
                <a href="@MainMenu.Dashboard.Path">
                    <i class="fa fa-pie-chart"></i> <span>@MainMenu.Dashboard.PageName</span>
                </a>
            </li>
            <li class="header">SALES</li>

            <li class="@Html.IsSelected(controller: MainMenu.CustomerType.ControllerName)">
                <a href="@MainMenu.CustomerType.Path">
                    <i class="fa fa-users"></i><span>@MainMenu.CustomerType.PageName</span>
                </a>
            </li>
            <li class="@Html.IsSelected(controller: MainMenu.Customer.ControllerName)">
                <a href="@MainMenu.Customer.Path">
                    <i class="fa fa-user"></i> <span>@MainMenu.Customer.PageName</span>
                </a>
            </li>          
          
        </ul>
    </section>
    <!-- /.sidebar -->
</aside>